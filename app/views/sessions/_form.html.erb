<h1>Formularz</h1>
<%= form_for(:session, url: login_user_path) do |form| %>

  <div class="field">
    <%= form.label 'Login' %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label 'Hasło' %>
    <%= form.password_field :pass %>
  </div>

  <div class="field">
    <%= form.label 'Zaloguj jako:' %>
    <%= form.select :type, options_for_select([['Czytelnik', 'Reader'], ['Pracownik', 'Employee'], ['Administrator', 'Admin']])  %>
  </div>

  <div class="actions">
    <%= form.submit 'Zaloguj'%>
  </div>
<% end %>
