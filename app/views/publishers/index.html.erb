<p id="notice"><%= notice %></p>

<h1>Wydawnictwa</h1>

<table>
  <thead>
    <tr>
      <th>Nazwa</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @publishers.each do |publisher| %>
      <tr>
        <td><%= publisher.nazwa %></td>
        <td><%= link_to 'Pokaż', publisher %></td>
        <td><%= link_to 'Edytuj', edit_publisher_path(publisher) %></td>
        <td><%= link_to 'Usuń', publisher, method: :delete, data: { confirm: 'Jesteś pewien?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Dodaj Wydawnictwo', new_publisher_path %>
